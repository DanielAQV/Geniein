# ── 비밀·자격증명 ────────────────────────────────
.env
.env.*
!.env.example
*.pem
*.key
*.cer
# 뇌의 SharePoint 인증서가 사는 곳 (개인키 포함)
secrets/

# ── 사내 문서 (절대 커밋 금지) ──────────────────────
# 사규·가이드 원본. RAG ingest 입력이지 소스코드가 아니다.
regulations/
uploads/

# ── 의존성 ──────────────────────────────────────
node_modules/
.pnpm-store/
__pycache__/
*.py[cod]
.venv/
venv/

# ── 빌드 산출물 ──────────────────────────────────
.next/
dist/
build/
*.tsbuildinfo

# ── 런타임 데이터 ────────────────────────────────
pgdata/
apps/web/public/uploads/

# ── 에디터·OS ────────────────────────────────────
.DS_Store
Thumbs.db
.idea/
*.swp

# ── 로컬 도구 ────────────────────────────────────
.claude/settings.local.json
coverage/

# Next 16 이 dev 와 build 에서 서로 다르게 쓰는 자동 생성 파일이다.
#   next dev   → import "./.next/dev/types/routes.d.ts"
#   next build → import "./.next/types/routes.d.ts"
# 추적하면 개발기와 빌드서버가 만날 때마다 pull 이 막힌다. 어차피 매 실행마다
# Next 가 다시 만든다.
next-env.d.ts
